# PolymorphicActivity sample

This sample demonstrates the use of Activities that extend a common interface.

From the root of the project, run the following command:

```bash
php ./app/app.php polymorphic
```
